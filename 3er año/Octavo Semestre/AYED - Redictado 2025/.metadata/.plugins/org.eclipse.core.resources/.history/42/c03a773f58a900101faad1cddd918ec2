package tp2.BinaryTree;

import java.util.ArrayList;

public class punto8 {

	public boolean esPrefijo(BinaryTree<Integer> ab1, BinaryTree<Integer> ab2) {
		if (ab1.isEmpty() || ab2.isEmpty()) {
			return false;
		}
		ArrayList<Integer> l1 = new ArrayList<Integer>();
		ArrayList<Integer> l2 = new ArrayList<Integer>();
		l1 = recorrido(ab1);
		l2 = recorrido(ab2)''
		return esPrefijo(ab1, ab2, l1, l2);
	}
	
	private boolean esPrefijo(BinaryTree<Integer> ab1, BinaryTree<Integer> ab2, ArrayList<Integer> l1, ArrayList<Integer> l2) {
		Queue<BinaryTree<>>
	}

}
