package parciales;

import java.util.LinkedList;

public class parcialdragonprincesa {

	public LinkedList<String> princesaAccesible(BinaryTree<Integer> arbol) {
		LinkedList<String> lista = new LinkedList<String>();
		LinkedList<String> listaMax = new LinkedList<String>();
		if (arbol != null && !arbol.isEmpty()) {
			recorrido(arbol, lista, listaMax);
		}
		return listaMax;
	}
	
	private void recorrido(BinaryTree<Integer> arbol, LinkedList<String> lista, LinkedList<String> listaMax) {
		if (arbol.getData().equals("Princesa")) {
			if (lista.size() <)
		}
	}
	
}
